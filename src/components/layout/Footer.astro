---
import { footerNavigation } from '@data/navigation';
import { siteConfig } from '@data/seo-config';

const currentYear = new Date().getFullYear();
---

<footer class="bg-secondary-900 text-secondary-300">
  <div class="container-wide py-12 md:py-16">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8 lg:gap-12">
      <!-- Brand -->
      <div class="lg:col-span-1">
        <a href="/" class="flex items-center space-x-2.5 text-white mb-4">
          <svg class="w-8 h-8" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M12 2L4 7v2h16V7L12 2z" fill="currentColor" opacity="0.15"/>
            <path d="M4 9v11h16V9" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M12 2L4 7h16L12 2z" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M7 13h2v4H7zM11 13h2v4h-2zM15 13h2v4h-2z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            <path d="M4 20h16" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
          </svg>
          <span class="font-serif font-bold text-lg">{siteConfig.name}</span>
        </a>
        <p class="text-secondary-400 text-sm leading-relaxed">
          Ressource d'information sur la succession et l'héritage en France. Démarches, droits, frais de notaire, simulateurs et questions fréquentes.
        </p>
      </div>

      <!-- Pages Piliers -->
      <div>
        <h3 class="text-white font-semibold mb-4">Thématiques</h3>
        <ul class="space-y-2">
          {footerNavigation.piliers.map((link) => (
            <li>
              <a href={link.href} class="text-secondary-400 hover:text-white transition-colors text-sm">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Ressources -->
      <div>
        <h3 class="text-white font-semibold mb-4">Ressources</h3>
        <ul class="space-y-2">
          {footerNavigation.ressources.map((link) => (
            <li>
              <a href={link.href} class="text-secondary-400 hover:text-white transition-colors text-sm">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Legal -->
      <div>
        <h3 class="text-white font-semibold mb-4">Informations</h3>
        <ul class="space-y-2">
          {footerNavigation.legal.map((link) => (
            <li>
              <a href={link.href} class="text-secondary-400 hover:text-white transition-colors text-sm">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Bottom -->
    <div class="mt-12 pt-8 border-t border-secondary-800">
      <div class="flex flex-col md:flex-row justify-between items-center gap-4">
        <p class="text-secondary-500 text-sm">
          &copy; {currentYear} {siteConfig.name}. Tous droits réservés.
        </p>
        <p class="text-secondary-500 text-xs">
          Les informations présentes sur ce site sont fournies à titre informatif et ne sauraient se substituer à un conseil juridique personnalisé. Consultez un notaire ou un avocat pour toute démarche.
        </p>
      </div>
    </div>
  </div>
</footer>
