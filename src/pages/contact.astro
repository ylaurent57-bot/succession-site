---
import BaseLayout from '@layouts/BaseLayout.astro';
---

<BaseLayout
  title="Contact - Succession France"
  description="Contactez l'équipe Succession France. Questions, suggestions, partenariats : nous sommes à votre écoute."
  canonical="/contact/"
>
  <section class="py-12 md:py-16">
    <div class="container-narrow">
      <header class="text-center mb-12">
        <h1 class="mb-4">Nous contacter</h1>
        <p class="text-xl text-secondary-600">
          Une question ? Une suggestion ? Écrivez-nous !
        </p>
      </header>

      <div class="grid md:grid-cols-2 gap-12">
        <!-- Formulaire -->
        <div>
          <form id="contact-form" class="space-y-6">
            <div>
              <label for="name" class="block text-sm font-medium text-secondary-700 mb-2">
                Votre nom
              </label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 rounded-lg border border-secondary-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none"
              />
            </div>

            <div>
              <label for="email" class="block text-sm font-medium text-secondary-700 mb-2">
                Votre email
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 rounded-lg border border-secondary-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none"
              />
            </div>

            <div>
              <label for="subject" class="block text-sm font-medium text-secondary-700 mb-2">
                Sujet
              </label>
              <select
                id="subject"
                name="subject"
                class="w-full px-4 py-3 rounded-lg border border-secondary-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none"
              >
                <option value="question">Question sur un article</option>
                <option value="suggestion">Suggestion de contenu</option>
                <option value="error">Signaler une erreur</option>
                <option value="partnership">Partenariat</option>
                <option value="other">Autre</option>
              </select>
            </div>

            <div>
              <label for="message" class="block text-sm font-medium text-secondary-700 mb-2">
                Votre message
              </label>
              <textarea
                id="message"
                name="message"
                rows="5"
                required
                class="w-full px-4 py-3 rounded-lg border border-secondary-300 focus:border-primary-500 focus:ring-2 focus:ring-primary-200 outline-none resize-none"
              ></textarea>
            </div>

            <button type="submit" class="btn-primary w-full">
              Envoyer le message
            </button>
          </form>
        </div>

        <!-- Informations -->
        <div class="space-y-8">
          <div class="card">
            <h2 class="text-lg font-semibold text-secondary-900 mb-4">Avant de nous écrire</h2>
            <p class="text-secondary-600 mb-4">
              Nous recevons de nombreux messages. Pour une réponse plus rapide, consultez d'abord :
            </p>
            <ul class="space-y-2 text-secondary-600">
              <li>
                <a href="/succession/" class="text-primary-600 hover:underline">Notre guide sur la succession</a>
              </li>
              <li>
                <a href="/outils/" class="text-primary-600 hover:underline">Nos outils de calcul</a>
              </li>
              <li>
                <a href="/blog/" class="text-primary-600 hover:underline">Nos articles de blog</a>
              </li>
            </ul>
          </div>

          <div class="card">
            <h2 class="text-lg font-semibold text-secondary-900 mb-4">Important</h2>
            <p class="text-secondary-600">
              <strong>Succession France</strong> est un site d'information. Nous ne sommes pas notaires ni avocats et ne pouvons pas donner de conseils juridiques personnalisés.
            </p>
            <p class="text-secondary-600 mt-2">
              Pour une question sur votre situation personnelle, consultez un professionnel du droit.
            </p>
          </div>

          <div class="card">
            <h2 class="text-lg font-semibold text-secondary-900 mb-4">Délai de réponse</h2>
            <p class="text-secondary-600">
              Nous nous efforçons de répondre à tous les messages dans un délai de <strong>48 à 72 heures</strong> (jours ouvrés).
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();

    const button = form.querySelector('button[type="submit"]') as HTMLButtonElement;
    button.textContent = 'Envoi en cours...';
    button.disabled = true;

    // Simuler l'envoi (remplacer par votre API)
    await new Promise((resolve) => setTimeout(resolve, 1500));

    form.innerHTML = `
      <div class="text-center py-8">
        <svg class="w-16 h-16 text-green-500 mx-auto mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
        </svg>
        <h3 class="text-xl font-semibold text-secondary-900 mb-2">Message envoyé !</h3>
        <p class="text-secondary-600">Nous vous répondrons dans les meilleurs délais.</p>
      </div>
    `;
  });
</script>
